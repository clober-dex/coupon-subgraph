type Substitute @entity {
  id: ID!
  symbol: String!
  name: String!
  decimals: BigInt!

  underlying: Token!
}

type Token @entity {
  id: ID!
  symbol: String!
  name: String!
  decimals: BigInt!

  substitutes: [Substitute!]! @derivedFrom(field: "underlying")
}